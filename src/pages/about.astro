---
import '../styles/global.css';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
---

<html lang="en">
  <head>
    <style>
      main {
        margin-top: 2rem;
      }
      .avatar-container {
        display: flex;
        justify-content: center;
        margin-bottom: 2em;
      }

      .about-me-container {
        text-align: center;
        margin: 3em auto 4em auto;
        max-width: 700px;
        padding: 0 2em;
      }
      .about-me-container h2 {
        font-size: 2.5rem;
        margin-bottom: 0.5em;
        background: linear-gradient(90deg, #00fff7, #ff6ec7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }
      .subtitle {
        font-size: 1.5rem;
        color: var(--neon-blue);
        font-family: var(--retro-font);
        margin-bottom: 1em;
      }
      .description {
        font-size: 1.2rem;
        color: rgb(var(--gray));
        line-height: 1.8;
        margin-bottom: 1.5em;
      }
      .description p {
        margin-bottom: 1.2em;
      }
      .cards-container {
        display: flex;
        flex-direction: column;
        gap: 2em;
        max-width: 1200px;
        margin: 0 auto 2.5em auto;
        padding: 0 2em;
      }
      .top-cards-row {
        display: flex;
        flex-direction: column;
        gap: 2em;
      }
      .stats-card {
        background: rgba(10, 10, 20, 0.82);
        border: 1.5px solid rgba(57, 255, 20, 0.18);
        border-radius: 18px;
        box-shadow:
          0 2px 16px 0 rgba(57, 255, 20, 0.08),
          0 0 0 1.5px rgba(57, 255, 20, 0.12) inset;
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        padding: 2em;
      }
      .stats-card h3 {
        color: var(--neon-green);
        font-family: var(--header-font);
        margin-top: 0;
      }
      .stats-list {
        list-style: none;
        padding: 0;
        margin: 0;
        color: var(--neon-blue);
        font-family: var(--retro-font);
        font-size: 1.08rem;
      }
      .skills-container {
        background: rgba(10, 10, 20, 0.82);
        border: 1.5px solid rgba(58, 170, 255, 0.13);
        border-radius: 18px;
        box-shadow: 0 2px 16px 0 rgba(58, 170, 255, 0.08);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        padding: 2em;
      }
      .skills-container h3 {
        color: var(--neon-blue);
        font-family: var(--header-font);
        margin-top: 0;
      }
      .skills-list-container {
        display: flex;
        flex-wrap: wrap;
        gap: 1.5em;
      }
      .skills-list {
        flex: 1 1 180px;
        min-width: 180px;
        color: var(--neon-green);
        font-family: var(--retro-font);
        font-size: 1.08rem;
        margin: 0;
      }
      .now-container {
        background: rgba(10, 10, 20, 0.82);
        border: 1.5px solid rgba(57, 255, 20, 0.13);
        border-radius: 18px;
        box-shadow: 0 2px 16px 0 rgba(57, 255, 20, 0.08);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        padding: 2em;
      }
      .now-container h3 {
        color: var(--neon-green);
        font-family: var(--header-font);
        margin-top: 0;
      }
      .now-container p {
        color: rgb(var(--gray));
        font-size: 1.08rem;
      }
      .interests-card {
        background: rgba(10, 10, 20, 0.82);
        border: 1.5px solid rgba(255, 111, 199, 0.18);
        border-radius: 18px;
        box-shadow: 0 2px 16px 0 rgba(255, 111, 199, 0.08);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        padding: 2em;
      }
      .interests-card h3 {
        color: #ff6ec7;
        font-family: var(--header-font);
        margin-top: 0;
      }
      .interests-list {
        display: flex;
        flex-wrap: wrap;
        gap: 1em;
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .interests-list li {
        color: var(--neon-green);
        font-family: var(--retro-font);
        font-size: 1.08rem;
        padding: 0.5em 1em;
        background: rgba(57, 255, 20, 0.1);
        border: 1px solid rgba(57, 255, 20, 0.2);
        border-radius: 12px;
      }
      @media (min-width: 900px) {
        .top-cards-row {
          flex-direction: row;
        }
        .stats-card {
          flex: 1;
        }
        .skills-container {
          flex: 1;
        }
      }
      @media (max-width: 720px) {
        .about-me-container h2 {
          font-size: 2rem;
        }
        .subtitle {
          font-size: 1.2rem;
        }
        .description {
          font-size: 1rem;
        }
        .cards-container {
          padding: 0 1em;
        }
      }
    </style>
    <BaseHead title="Joonas.dev - About Me" description="About me" />
  </head>
  <body>
    <Header />
    <main>
      <div class="about-me-container">
        <h2>Moro! I'm Joonas 👋</h2>
        <div class="subtitle">Developer, Generalist & Digital Tinkerer</div>
        <div class="description">
          <p>
            I'm a web developer with over 10 years of experience building scalable, high-quality
            projects from the ground up to the production stage.
          </p>
          <p>
            While the web is my core domain, I consider myself to be a generalist. I enjoy building
            things - whether it's a web app, mobile app, game, or even embedded electronics projects
            - and that curiosity drives me to constantly learn new technologies. When I come up with
            a new idea, I dive in and figure out how to bring it to life.
          </p>
          <p>
            I love solving problems, experimenting with new ideas, and crafting products that feel
            great to use.
          </p>
        </div>
      </div>

      <div class="cards-container">
        <div class="top-cards-row">
          <div class="stats-card">
            <h3>Quick Stats</h3>
            <ul class="stats-list">
              <li>- Based in Oulu, Finland</li>
              <li>- Coding since 2001</li>
              <li>
                - Experience in web development, UI/UX design, mobile development, game development,
                embedded systems, and more
              </li>
            </ul>
          </div>

          <div class="skills-container">
            <h3>Skills & Tools</h3>
            <div class="skills-list-container">
              <ul class="skills-list">
                <li>TypeScript / JavaScript</li>
                <li>React/Vue/Angular</li>
                <li>CSS / Sass / Tailwind</li>
                <li>Node.js / Python / Go / Godot</li>
                <li>React Native</li>
                <li>AWS / GCP</li>
              </ul>
            </div>
          </div>
        </div>

        <!-- <div class="now-container">
          <h3>Now</h3>
          <p></p>
        </div> -->

        <div class="interests-card">
          <h3>Interests & Hobbies</h3>
          <ul class="interests-list">
            <li>Video Games</li>
            <li>Synthwave music</li>
            <li>Movies</li>
            <li>Gadgets and electronics</li>
          </ul>
        </div>
      </div>
      <Footer />
    </main>
  </body>
</html>
